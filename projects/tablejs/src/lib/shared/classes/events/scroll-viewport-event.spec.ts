import { ScrollViewportEvent } from './scroll-viewport-event';

describe('ScrollViewportEvent', () => {
  it('should create an instance', () => {
    expect(new ScrollViewportEvent()).toBeTruthy();
  });
});
